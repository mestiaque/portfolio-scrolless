`<section class="cux-shell" id="contact-page">
	<div class="cux-grid">
		<aside class="cux-left slide-in-left">
			<div class="cux-head">
				<p class="cux-eyebrow">Contact Hub</p>
				<h2 class="d-lg">Get in <em class="c">Touch</em></h2>
				<p>Choose any channel below. Every card is live and actionable.</p>
			</div>

			<a class="cux-info-card" href="tel:+8801234567890" aria-label="Call phone number">
				<span class="cux-icon" aria-hidden="true">
					<svg viewBox="0 0 24 24"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.8 19.8 0 0 1-8.63-3.07 19.52 19.52 0 0 1-6-6 19.8 19.8 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.12.9.34 1.78.65 2.62a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.46-1.22a2 2 0 0 1 2.11-.45c.84.31 1.72.53 2.62.65A2 2 0 0 1 22 16.92z"/></svg>
				</span>
				<span class="cux-info-copy">
					<strong>Phone</strong>
					<small>+880 1234 567 890</small>
				</span>
			</a>

			<a class="cux-info-card" href="mailto:hello@me.dev" aria-label="Send email">
				<span class="cux-icon" aria-hidden="true">
					<svg viewBox="0 0 24 24"><path d="M4 4h16a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2zm0 2 8 6 8-6"/></svg>
				</span>
				<span class="cux-info-copy">
					<strong>Email</strong>
					<small>hello@me.dev</small>
				</span>
			</a>

			<a class="cux-info-card" href="https://maps.google.com/?q=Dhaka,Bangladesh" target="_blank" rel="noopener noreferrer" aria-label="Open map address">
				<span class="cux-icon" aria-hidden="true">
					<svg viewBox="0 0 24 24"><path d="M12 22s7-6.2 7-12a7 7 0 1 0-14 0c0 5.8 7 12 7 12zm0-9.5A2.5 2.5 0 1 1 12 7a2.5 2.5 0 0 1 0 5.5z"/></svg>
				</span>
				<span class="cux-info-copy">
					<strong>Address</strong>
					<small>Uttara, Dhaka, Bangladesh</small>
				</span>
			</a>

			<a class="cux-info-card" href="https://t.me/yourusername" target="_blank" rel="noopener noreferrer" aria-label="Open Telegram chat">
				<span class="cux-icon" aria-hidden="true">
					<svg viewBox="0 0 24 24"><path d="M21.5 3.5 2.8 10.7c-1 .4-1 1.8.1 2.2l4.6 1.5 1.7 5.2c.3.8 1.3 1.1 2 .5l2.6-2.2 4.6 3.4c.8.6 2 .2 2.2-.8L23 4.9c.2-1-.8-1.8-1.7-1.4zM9.1 13.8 18.8 7l-7.7 7.9-.3 3.1-1.7-4.2z"/></svg>
				</span>
				<span class="cux-info-copy">
					<strong>Telegram</strong>
					<small>@yourusername</small>
				</span>
			</a>

			<a class="cux-info-card" href="https://wa.me/8801234567890" target="_blank" rel="noopener noreferrer" aria-label="Open WhatsApp chat">
				<span class="cux-icon" aria-hidden="true">
					<svg viewBox="0 0 24 24"><path d="M20 11.5A8.5 8.5 0 0 1 7.7 19L3 20.3l1.4-4.6A8.5 8.5 0 1 1 20 11.5zm-4.8 2.3c-.2-.1-1.1-.5-1.3-.6s-.3-.1-.5.1-.6.6-.7.7-.3.1-.5 0a7 7 0 0 1-2.1-1.3 7.6 7.6 0 0 1-1.4-1.8c-.1-.2 0-.4.1-.5l.3-.3.2-.4c.1-.1.1-.3 0-.4l-.6-1.5c-.2-.4-.4-.4-.5-.4h-.4c-.2 0-.4.1-.5.3-.2.2-.8.8-.8 2s.8 2.3.9 2.5a9.3 9.3 0 0 0 3.6 3.2c1.6.6 2.2.6 3 .5.5-.1 1.5-.6 1.7-1.2.2-.5.2-1 .1-1.1-.2-.1-.4-.2-.6-.3z"/></svg>
				</span>
				<span class="cux-info-copy">
					<strong>WhatsApp</strong>
					<small>Chat Instantly</small>
				</span>
			</a>
		</aside>

		<div class="cux-right slide-in-right">
			<article class="cux-msg-card" id="cux-msg-card">
				<div class="cux-head" style="margin-bottom: 1rem">
					<p class="cux-eyebrow">Leave a Message</p>
					<h2 class="d-lg">Say <em class="r">Hello</em></h2>
					<p>Drop your message below, and I'll get back to you soon.</p>
				</div>

				<form id="cux-form" novalidate>
					<div class="cux-fields" id="cux-fields">
						<label class="cux-field" for="cux-name">
							<span>Name</span>
							<input id="cux-name" name="name" type="text" placeholder="Your full name" required>
						</label>

						<label class="cux-field" for="cux-email">
							<span>Email</span>
							<input id="cux-email" name="email" type="email" placeholder="you@example.com" required>
						</label>

						<label class="cux-field" for="cux-subject">
							<span>Subject</span>
							<input id="cux-subject" name="subject" type="text" placeholder="Project details" required>
						</label>

						<label class="cux-field" for="cux-message">
							<span>Message</span>
							<textarea id="cux-message" name="message" placeholder="Write your message here..." rows="4" required></textarea>
						</label>

						<button type="submit" class="cux-btn" id="cux-submit">Send Message</button>
					</div>

					<div class="cux-success" id="cux-success" aria-live="polite">
						<span class="cux-check" aria-hidden="true">
							<svg viewBox="0 0 52 52">
								<circle cx="26" cy="26" r="24"></circle>
								<path d="M15 27 23 35 38 19"></path>
							</svg>
						</span>
						<h4>Message Sent</h4>
						<p>Thank you. The form will reset automatically in 1 minute.</p>
					</div>
				</form>
			</article>
		</div>
	</div>
</section>

<style>
	.cux-shell {
		--c-bg-1: #050b16;
		--c-bg-2: #0c1a2f;
		--c-line: #17365f;
		--c-glow: #2ca7ff;
		--c-text: #eff7ff;
		--c-mute: #9eb6d2;
		--c-soft: #6f89a8;
		width: min(1200px, 96%);
		margin: 2rem auto;
		position: relative;
		isolation: isolate;
		/* border: 1px solid rgba(138, 189, 240, .24); */
		border-radius: 28px;
		background: linear-gradient(140deg, rgba(8, 20, 40, .22), rgba(8, 24, 44, .1));
		backdrop-filter: blur(4px) saturate(120%);
	}

	.cux-shell::before {
		content: '';
		position: absolute;
		z-index: -1;
		border-radius: 28px;
		background:
			radial-gradient(circle at 10% 10%, rgba(49, 161, 255, .14), transparent 35%),
			radial-gradient(circle at 90% 80%, rgba(52, 228, 201, .14), transparent 30%),
			linear-gradient(140deg, rgba(255, 255, 255, .03), rgba(255, 255, 255, 0));
		filter: blur(2px);
	}

	.cux-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.1rem;
	}

	.cux-left,
	.cux-msg-card {
		border: 1px solid rgba(159, 201, 248, .28);
		border-radius: 22px;
		background:
			linear-gradient(165deg, rgba(36, 63, 103, .33), rgba(7, 20, 38, .2));
		backdrop-filter: blur(14px) saturate(135%);
		box-shadow: inset 0 1px 0 rgba(255, 255, 255, .09), 0 12px 30px rgba(0, 0, 0, .25);
	}

	.cux-left {
		padding: 1.15rem;
		display: grid;
		gap: .75rem;
	}

	.cux-head {
		margin-bottom: .35rem;
	}

	.cux-eyebrow {
		font-size: .66rem;
		letter-spacing: .17em;
		text-transform: uppercase;
		color: var(--c-soft);
		margin-bottom: .35rem;
	}

	.cux-head h2 {
		font-size: clamp(1.35rem, 2.3vw, 2rem);
		color: var(--c-text);
		line-height: 1.12;
		margin-bottom: .45rem;
	}

	.cux-head p {
		color: var(--c-mute);
		font-size: .9rem;
		line-height: 1.6;
		/* max-width: 44ch; */
	}

	.cux-info-card {
		display: grid;
		grid-template-columns: 52px 1fr;
		gap: .8rem;
		align-items: center;
		padding: .75rem .85rem;
		border-radius: 14px;
		border: 1px solid rgba(119, 173, 237, .22);
		background: linear-gradient(140deg, rgba(18, 45, 78, .32), rgba(8, 16, 31, .16));
		text-decoration: none;
		transition: transform .26s ease, border-color .26s ease, box-shadow .26s ease;
	}

	.cux-info-card:hover {
		transform: translateY(-2px);
		border-color: rgba(121, 197, 255, .58);
		box-shadow: 0 0 0 1px rgba(77, 172, 255, .24), 0 14px 30px rgba(2, 10, 25, .45);
	}

	.cux-icon {
		width: 52px;
		height: 52px;
		border-radius: 12px;
		display: grid;
		place-items: center;
		border: 1px solid rgba(89, 170, 255, .35);
		background: radial-gradient(circle at 30% 20%, rgba(46, 163, 255, .24), rgba(8, 26, 50, .7));
	}

	.cux-icon svg {
		width: 24px;
		height: 24px;
		fill: none;
		stroke: #35c0ff;
		stroke-width: 1.9;
		stroke-linecap: round;
		stroke-linejoin: round;
	}

	.cux-info-copy strong {
		display: block;
		color: var(--c-text);
		font-size: 1rem;
		margin-bottom: .08rem;
	}

	.cux-info-copy small {
		color: var(--c-mute);
		font-size: .84rem;
		line-height: 1.45;
	}

	.cux-right {
		min-height: 100%;
	}

	.cux-msg-card {
		height: 100%;
		padding: 1.35rem;
		position: relative;
		overflow: hidden;
	}

	.cux-msg-card::after {
		content: '';
		position: absolute;
		width: 240px;
		height: 240px;
		border-radius: 50%;
		right: -100px;
		top: -110px;
		background: radial-gradient(circle, rgba(56, 165, 255, .32), transparent 62%);
		pointer-events: none;
	}

	.cux-msg-head p {
		font-size: .7rem;
		letter-spacing: .14em;
		text-transform: uppercase;
		color: var(--c-soft);
	}

	.cux-msg-head h3 {
		font-size: clamp(1.6rem, 2.8vw, 2.35rem);
		color: var(--c-text);
		margin-top: .2rem;
		margin-bottom: 1.1rem;
		line-height: 1.1;
	}

	.cux-fields {
		display: grid;
		gap: .82rem;
	}

	.cux-field {
		display: grid;
		gap: .32rem;
		transition: transform .5s ease, opacity .5s ease, filter .5s ease;
	}

	.cux-field span {
		font-size: .78rem;
		color: #bdd1e9;
		letter-spacing: .2em;
		text-transform: uppercase;
	}

	.cux-field input {
		width: 100%;
		border: 1px solid rgba(110, 162, 224, .35);
		border-radius: 12px;
		padding: .8rem .88rem;
		background: rgba(8, 21, 41, .34);
		color: #f2f8ff;
		font-size: .94rem;
		transition: border-color .2s ease, box-shadow .2s ease;
	}

	.cux-field textarea {
		width: 100%;
		resize: vertical;
		min-height: 110px;
		border: 1px solid rgba(110, 162, 224, .35);
		border-radius: 12px;
		padding: .8rem .88rem;
		background: rgba(8, 21, 41, .34);
		color: #f2f8ff;
		font-size: .94rem;
		line-height: 1.55;
		font-family: inherit;
		transition: border-color .2s ease, box-shadow .2s ease;
	}

	.cux-field input::placeholder,
	.cux-field textarea::placeholder {
		color: #86a5c8;
	}

	.cux-field input:focus,
	.cux-field textarea:focus {
		outline: 0;
		border-color: rgba(104, 191, 255, .85);
		box-shadow: 0 0 0 3px rgba(76, 172, 255, .2);
	}

	.cux-btn {
		border: 1px solid rgba(255, 255, 255, 0.12); /* কাঁচের হালকা বর্ডার */
		border-radius: 12px;
		padding: .9rem 1rem;
		font-size: .85rem;
		letter-spacing: .09em;
		text-transform: uppercase;
		font-weight: 700;
		
		/* টেক্সটের কালার সাদা বা উজ্জ্বল হওয়া দরকার যেন ডার্ক ব্যাকগ্রাউন্ডে ফুটে ওঠে */
		color: #ffffff; 
		
		/* গ্লাস ইফেক্ট এবং লিকুইড গ্রেডিয়েন্ট ব্যাকগ্রাউন্ড */
		background: linear-gradient(135deg, rgba(50, 204, 255, 0.15), rgba(112, 103, 242, 0.15));
		backdrop-filter: blur(12px); /* পেছনের অংশ ঝাপসা করার ম্যাজিক লাইন */
		-webkit-backdrop-filter: blur(12px);
		
		cursor: pointer;
		transition: all .3s cubic-bezier(0.25, 0.8, 0.25, 1);
		
		/* বাটনের ভেতরে হালকা লিকুইড গ্লো বা শাইন */
		box-shadow: inset 0 1px 1px rgba(255, 255, 255, 0.2), 
					0 4px 15px rgba(0, 0, 0, 0.2);
	}

	.cux-btn:hover {
		transform: translateY(-2px);
		
		/* হোভার করলে লিকুইড গ্রেডিয়েন্টটি আরও উজ্জ্বল ও স্বচ্ছ হবে */
		background: linear-gradient(135deg, rgba(50, 204, 255, 0.3), rgba(112, 103, 242, 0.3));
		border-color: rgba(50, 204, 255, 0.4);
		
		/* বাইরের নিয়ন গ্লো ইফেক্ট */
		box-shadow: inset 0 1px 2px rgba(255, 255, 255, 0.4),
					0 10px 30px rgba(68, 103, 255, 0.4);
	}

	.cux-btn:active {
		transform: translateY(0);
	}

	.cux-btn:disabled {
		opacity: .35;
		background: rgba(255, 255, 255, 0.05);
		border-color: rgba(255, 255, 255, 0.05);
		color: rgba(255, 255, 255, 0.4);
		cursor: not-allowed;
		transform: none;
		box-shadow: none;
		backdrop-filter: blur(4px);
	}


	.cux-success {
		position: absolute;
		inset: 0;
		opacity: 0;
		pointer-events: none;
		display: grid;
		place-content: center;
		text-align: center;
		padding: 2rem 1.2rem;
		background: radial-gradient(circle at center, rgba(14, 43, 72, .34), rgba(8, 16, 30, .12));
		transition: opacity .45s ease;
	}

	.cux-check {
		width: 170px;
		height: 170px;
		margin: 0 auto 1rem;
		display: inline-block;
		border-radius: 50%;
		background: radial-gradient(circle, rgba(84, 255, 213, .14), rgba(84, 255, 213, 0) 70%);
		padding: 24px;
	}

	.cux-check svg {
		width: 100%;
		height: 100%;
		fill: none;
		stroke: #66ffc9;
		stroke-width: 2.8;
		filter: drop-shadow(0 0 22px rgba(92, 255, 200, .52));
	}

	.cux-check circle,
	.cux-check path {
		stroke-dasharray: 180;
		stroke-dashoffset: 180;
	}

	.cux-success h4 {
		font-size: 2rem;
		color: #ebfff7;
		margin-bottom: .35rem;
	}

	.cux-success p {
		color: #a3d3c4;
		font-size: 1.05rem;
		line-height: 1.55;
		max-width: 34ch;
	}

	.cux-msg-card.sent .cux-field,
	.cux-msg-card.sent .cux-btn {
		opacity: 0;
		transform: translateY(-18px) scale(.97);
		filter: blur(2px);
		pointer-events: none;
	}

	.cux-msg-card.sent .cux-field:nth-child(1) { transition-delay: .05s; }
	.cux-msg-card.sent .cux-field:nth-child(2) { transition-delay: .15s; }
	.cux-msg-card.sent .cux-field:nth-child(3) { transition-delay: .25s; }
	.cux-msg-card.sent .cux-field:nth-child(4) { transition-delay: .35s; }
	.cux-msg-card.sent .cux-btn { transition-delay: .45s; }

	.cux-msg-card.sent .cux-success {
		opacity: 1;
		pointer-events: auto;
		transition-delay: .45s;
	}

	.cux-msg-card.sent .cux-check circle {
		animation: cux-draw .7s ease forwards .55s;
	}

	.cux-msg-card.sent .cux-check path {
		animation: cux-draw .45s ease forwards 1.1s;
	}

	.cux-field.is-error input,
	.cux-field.is-error textarea {
		border-color: #ff6b8f;
		box-shadow: 0 0 0 3px rgba(255, 107, 143, .18);
	}

	@keyframes cux-draw {
		to {
			stroke-dashoffset: 0;
		}
	}

	@media (max-width: 960px) {
		.cux-grid {
			grid-template-columns: 1fr;
		}
		.cux-shell {
			width: min(700px, 96%);
		}
	}

	@media (max-width: 620px) {
		.cux-shell {
			margin: 1.25rem auto;
		}
		.cux-left,
		.cux-msg-card {
			padding: .9rem;
			border-radius: 18px;
		}
		.cux-info-card {
			grid-template-columns: 46px 1fr;
			padding: .68rem .7rem;
		}
		.cux-icon {
			width: 46px;
			height: 46px;
		}
		.cux-head p,
		.cux-info-copy small,
		.cux-success p {
			font-size: .82rem;
		}
		.cux-field input {
			padding: .72rem .78rem;
			font-size: .88rem;
		}
		.cux-field textarea {
			padding: .72rem .78rem;
			font-size: .88rem;
			min-height: 95px;
		}
		.cux-btn {
			padding: .78rem .9rem;
			font-size: .76rem;
		}
		.cux-check {
			width: 120px;
			height: 120px;
			padding: 16px;
		}
		.cux-success h4 {
			font-size: 1.5rem;
		}
		.cux-success p {
			font-size: .9rem;
		}
	}
</style>

<script>
	(function () {
		var form = document.getElementById('cux-form');
		if (!form) return;

		var card = document.getElementById('cux-msg-card');
		var fields = Array.prototype.slice.call(form.querySelectorAll('.cux-field'));
		var button = document.getElementById('cux-submit');
		var resetTimer = null;

		function validate() {
			var ok = true;

			fields.forEach(function (field) {
				var input = field.querySelector('input, textarea');
				var value = input.value.trim();
				var valid = value.length > 0;

				if (input.type === 'email') {
					valid = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
				}

				field.classList.toggle('is-error', !valid);
				if (!valid) ok = false;
			});

			return ok;
		}

		function clearErrors() {
			fields.forEach(function (field) {
				field.classList.remove('is-error');
			});
		}

		function resetFormUI() {
			card.classList.remove('sent');
			button.disabled = false;
			form.reset();
			clearErrors();
			if (resetTimer) {
				clearTimeout(resetTimer);
				resetTimer = null;
			}
		}

		fields.forEach(function (field) {
			var input = field.querySelector('input, textarea');
			input.addEventListener('input', function () {
				field.classList.remove('is-error');
			});
		});

		function getDeviceInfo() {
			var nav = window.navigator || {};
			var conn = nav.connection || nav.mozConnection || nav.webkitConnection || null;
			var uaData = nav.userAgentData || null;

			return {
				user_agent: nav.userAgent || '',
				platform: nav.platform || '',
				vendor: nav.vendor || '',
				language: nav.language || '',
				languages: nav.languages || [],
				cookie_enabled: !!nav.cookieEnabled,
				online: !!nav.onLine,
				hardware_concurrency: nav.hardwareConcurrency || null,
				device_memory_gb: nav.deviceMemory || null,
				max_touch_points: nav.maxTouchPoints || 0,
				timezone: (window.Intl && Intl.DateTimeFormat) ? Intl.DateTimeFormat().resolvedOptions().timeZone : '',
				viewport: {
					width: window.innerWidth || null,
					height: window.innerHeight || null,
					pixel_ratio: window.devicePixelRatio || 1
				},
				screen: {
					width: window.screen ? screen.width : null,
					height: window.screen ? screen.height : null,
					avail_width: window.screen ? screen.availWidth : null,
					avail_height: window.screen ? screen.availHeight : null,
					color_depth: window.screen ? screen.colorDepth : null,
					pixel_depth: window.screen ? screen.pixelDepth : null,
					orientation: (window.screen && screen.orientation) ? screen.orientation.type : ''
				},
				connection: conn ? {
					effective_type: conn.effectiveType || '',
					downlink: conn.downlink || null,
					rtt: conn.rtt || null,
					save_data: !!conn.saveData
				} : null,
				ua_data: uaData ? {
					mobile: !!uaData.mobile,
					platform: uaData.platform || '',
					brands: uaData.brands || []
				} : null,
				referrer: document.referrer || '',
				page_url: window.location.href || ''
			};
		}

		form.addEventListener('submit', async function (event) {
			event.preventDefault();

			if (!validate()) return;

			button.disabled = true;

			var payload = {
				name: form.querySelector('[name="name"]').value.trim(),
				email: form.querySelector('[name="email"]').value.trim(),
				subject: form.querySelector('[name="subject"]').value.trim(),
				message: form.querySelector('[name="message"]').value.trim(),
				device_info: getDeviceInfo()
			};

			try {
				var tokenMeta = document.querySelector('meta[name="csrf-token"]');
				var response = await fetch('/api/messages-store', {
					method: 'POST',
					headers: {
						'Content-Type': 'application/json',
						'Accept': 'application/json',
						'X-Requested-With': 'XMLHttpRequest',
						'X-CSRF-TOKEN': tokenMeta ? tokenMeta.getAttribute('content') : ''
					},
					body: JSON.stringify(payload)
				});

				if (!response.ok) {
					button.disabled = false;
					return;
				}

				card.classList.add('sent');

				resetTimer = setTimeout(function () {
					resetFormUI();
				}, 30000);
			} catch (error) {
				button.disabled = false;
			}
		});
	})();
</script>
